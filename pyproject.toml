[project]
name = "darkomat-cloudflare"
version = "0.1.0"
description = "Dárkomat 2.0 - Cloudflare Python Worker"
requires-python = ">=3.13"

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
